---
description: Initialize a Git repository with an initial commit.
---
To execute this workflow, invoke the `speckit-git-initialize` skill. Read its instructions in `.agent/skills/speckit-git-initialize/SKILL.md` and follow them exactly.
