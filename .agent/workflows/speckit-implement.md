---
description: Execute the implementation plan by processing and executing all tasks defined in tasks.md.
---
To execute this workflow, invoke the `speckit-implement` skill. Read its instructions in `.agent/skills/speckit-implement/SKILL.md` and follow them exactly.
