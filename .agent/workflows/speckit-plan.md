---
description: Execute the implementation planning workflow using the plan template to generate design artifacts.
---
To execute this workflow, invoke the `speckit-plan` skill. Read its instructions in `.agent/skills/speckit-plan/SKILL.md` and follow them exactly.
