---
description: Auto-commit changes after a Spec Kit command completes.
---
To execute this workflow, invoke the `speckit-git-commit` skill. Read its instructions in `.agent/skills/speckit-git-commit/SKILL.md` and follow them exactly.
