---
description: Create or update the feature specification from a natural language feature description.
---
To execute this workflow, invoke the `speckit-specify` skill. Read its instructions in `.agent/skills/speckit-specify/SKILL.md` and follow them exactly.
