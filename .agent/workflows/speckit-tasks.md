---
description: Generate an actionable, dependency-ordered tasks.md for the feature based on available design artifacts.
---
To execute this workflow, invoke the `speckit-tasks` skill. Read its instructions in `.agent/skills/speckit-tasks/SKILL.md` and follow them exactly.
