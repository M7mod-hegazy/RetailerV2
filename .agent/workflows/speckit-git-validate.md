---
description: Validate current branch follows feature branch naming conventions.
---
To execute this workflow, invoke the `speckit-git-validate` skill. Read its instructions in `.agent/skills/speckit-git-validate/SKILL.md` and follow them exactly.
