---
description: Detect Git remote URL for GitHub integration.
---
To execute this workflow, invoke the `speckit-git-remote` skill. Read its instructions in `.agent/skills/speckit-git-remote/SKILL.md` and follow them exactly.
