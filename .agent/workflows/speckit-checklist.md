---
description: Generate a custom checklist for the current feature based on user requirements.
---
To execute this workflow, invoke the `speckit-checklist` skill. Read its instructions in `.agent/skills/speckit-checklist/SKILL.md` and follow them exactly.
