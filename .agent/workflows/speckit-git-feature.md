---
description: Create a feature branch with sequential or timestamp numbering.
---
To execute this workflow, invoke the `speckit-git-feature` skill. Read its instructions in `.agent/skills/speckit-git-feature/SKILL.md` and follow them exactly.
